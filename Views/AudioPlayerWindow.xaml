<Window x:Class="CallRecording.Views.AudioPlayerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="音频预览" Height="200" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding CurrentFileName}"
                   TextTrimming="CharacterEllipsis" Margin="0,0,0,10" />

        <Slider Grid.Row="1" Value="{Binding Position}"
                Maximum="{Binding Duration}"
                IsMoveToPointEnabled="True"
                Margin="0,0,0,10" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="⏮" Command="{Binding RewindCommand}"
                    Width="60" Margin="5,0" />
            <Button Content="{Binding PlayButtonText}"
                    Command="{Binding PlayPauseCommand}"
                    Width="80" Margin="5,0" />
            <Button Content="⏭" Command="{Binding ForwardCommand}"
                    Width="60" Margin="5,0" />
            <TextBlock Text="{Binding TimeDisplay}"
                       VerticalAlignment="Center" Margin="10,0" />
        </StackPanel>
    </Grid>
</Window>