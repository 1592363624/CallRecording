<Window x:Class="CallRecording.Views.AudioManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="录音文件管理" Height="550" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 搜索和筛选区域 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Width="200" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,10,0" />
            <ComboBox Width="120" ItemsSource="{Binding AudioFormats}"
                      SelectedItem="{Binding SelectedFormat}"
                      Margin="0,0,10,0" />
            <DatePicker SelectedDate="{Binding StartDate}" Margin="0,0,10,0" />
            <DatePicker SelectedDate="{Binding EndDate}" Margin="0,0,10,0" />
            <Button Content="搜索" Command="{Binding SearchCommand}" />
        </StackPanel>

        <!-- 文件列表 -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding AudioFiles}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="文件名" Binding="{Binding FileName}" Width="*" />
                <DataGridTextColumn Header="大小" Binding="{Binding FileSize}" Width="100" />
                <DataGridTextColumn Header="录制时间" Binding="{Binding RecordTime}" Width="180" />
                <DataGridTextColumn Header="格式" Binding="{Binding Format}" Width="80" />
                <DataGridTemplateColumn Header="操作" Width="240">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="播放"
                                        Command="{Binding DataContext.PlayCommand, 
                                        RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Margin="2" />
                                <Button Content="重命名"
                                        Command="{Binding DataContext.RenameCommand, 
                                        RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Margin="2" />
                                <!-- <Button Content="导出"  -->
                                <!--         Command="{Binding DataContext.ExportCommand,  -->
                                <!--         RelativeSource={RelativeSource AncestorType=DataGrid}}" -->
                                <!--         CommandParameter="{Binding}" -->
                                <!--         Margin="2"/> -->
                                <Button Content="删除"
                                        Command="{Binding DataContext.DeleteCommand, 
                                        RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Margin="2" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 状态栏 -->
        <StatusBar Grid.Row="2" Margin="0,10,0,0">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}" />
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="{Binding TotalFilesText}" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>